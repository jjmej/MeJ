import React from 'react';

// This page is a placeholder. 
// The middleware redirects coaches from /dashboard to /admin/dashboard, so this component never renders.
// It exists to satisfy the Next.js build process which expects a page component here.
export default function EntrenadorDashboardPlaceholder() {
    return null;
}
